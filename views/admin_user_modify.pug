doctype html
html(lang="ko")
    head 
        meta(charset="utf-8")
        title Hackingcamp CTF
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.1/dist/css/bootstrap.min.css", rel="stylesheet")
        link(href="https://cdn.jsdelivr.net/npm/startbootstrap-simple-sidebar@4.1.1/css/simple-sidebar.css", rel="stylesheet")
        script(src="https://cdn.jsdelivr.net/npm/jquery@3.3.1/dist/jquery.min.js")
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@4.1.1/dist/js/bootstrap.bundle.min.js")
    body
        #wrapper
            include admin_sidebar.pug


            #page-content-wrapper
                .container-fluid
                    h1 Hackingcamp CTF User Modification
                    P 사용자 관리 기능입니다. 여기서 User의 기본적인 사항들을 수정할 수 있습니다.  
                    
                    form(action="/secretjuju/user/" + user.no + "/modify", method="post" enctype="multipart/form-data")
                        div.form-group
                            label Email
                            input.form-control(type="text", name="email", value=user.email)
                        div.form-group
                            lable Nickname
                            input.form-control(type="text", name="nickname", value=user.nickname)
                        div.form-group
                            lable Password
                            input.form-control(type="text", name="password", placeholder="수정하지 않으려면 공란으로")

                        div.form-group
                            if user.admin === true
                                input(type="checkbox", class="form-check-input", name="admin", checked)
                            else 
                                input(type="checkbox", class="form-check-input", name="admin")
                            label &nbsp;admin

                        
                        br
                        div.row
                            .col-sm-1
                                button.btn.btn-primary.mb-2 Modify
                            .col-sm-1
                                a.btn.btn-info.mb-2(href="/secretjuju/user/") List
                            .col-sm-1
                                a.btn.btn-danger.mb-2(href="/secretjuju/user/"+user.no+"/realrudaganya") Delete

                    P Make sure to keep all page content within the 
                        code #page-content-wrapper
                        |.
                    a(href="#menu-toggle", class="btn btn-secondary", id="menu-toggle") Toggle Menu
                              
        script(type="text/javascript")
            | $("#menu-toggle").click(function(e) {
            |  e.preventDefault();
            |  $("#wrapper").toggleClass("toggled");
            | });