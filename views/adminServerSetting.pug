doctype html
html(lang="ko")
    head
        meta(charset="utf-8")
        title Hackingcamp CTF
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.1/dist/css/bootstrap.min.css", rel="stylesheet")
        link(href="https://cdn.jsdelivr.net/npm/startbootstrap-simple-sidebar@4.1.1/css/simple-sidebar.css", rel="stylesheet")
        script(src="https://cdn.jsdelivr.net/npm/jquery@3.3.1/dist/jquery.min.js")
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@4.1.1/dist/js/bootstrap.bundle.min.js")
    body
        #wrapper
            include admin_sidebar.pug


            #page-content-wrapper
                .container-fluid
                    h1 Hackingcamp CTF Sever Setting
                    p 서버에 관련된 정보를 관리할 수 있습니다.
                    form.form-group(action="/serverSetting", method="post")
                        input.form-control(type="datetime-local", name="endTime")
                    table.table
                        tr
                            th #
                            th nickname
                            th point
                            th Last Solve Time

                        - var rank = 1;
                        each user in users
                            tr
                                td #{rank}
                                td
                                    a(href="/secretjuju/user/" + user.no ) #{user.nickname}
                                if user.point == null
                                    td 0
                                else
                                    td #{user.point}
                                td #{user.lastsolvetime}
                            - rank++;
                    P Make sure to keep all page content within the
                        code #page-content-wrapper
                        |.
                    a(href="#menu-toggle", class="btn btn-secondary", id="menu-toggle") Toggle Menu

        script(type="text/javascript")
            | $("#menu-toggle").click(function(e) {
            |  e.preventDefault();
            |  $("#wrapper").toggleClass("toggled");
            | });