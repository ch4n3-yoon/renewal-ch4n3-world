doctype html
html(lang='en')
    head
        meta(charset='UTF-8')
        meta(name='viewport', content='width=device-width, initial-scale=1.0')
        meta(http-equiv='X-UA-Compatible', content='ie=edge')
        title ch4n3 world
        script(src='https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/js/bootstrap.min.js')
        script(src='https://cdn.jsdelivr.net/npm/jquery@3.3.1/dist/jquery.min.js')
        link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css')
        style.
            html,body {
                height: 100%;
            }
            body {
                display: table;
                margin: 0 auto;
            }
            .container {
                height: 100%;
                display: table-cell;
                vertical-align: middle;
            }
            center {
                height: 50px;
                width: 50px;
                background-color: black;
            }
            .header {
                margin-top: 100em;
                margin: 0 auto;
                text-align: center;
            }
            h1 {
                font-weight: bold;
            }
    body
        .container
            .header
                h1.text-uppercase Sign up for CH4N3 WORLD
            form(action='/register', method='post')
                .form-group
                    label(for='email')
                        | E-mail
                        span.text-danger *
                    .input-group
                        input#email.form-control.by-light(type='email', name='email', autocomplete='off')
                .form-group
                    label(for='password')
                        | Password
                        span.text-danger *
                    .input-group
                        input#password.form-control.by-light(type='password', name='password', autocomplete='off')
                    small.form-text.text-muted Password must be at least 6 characters.
                .form-group
                    label(for='nickname')
                        | Nickname
                        span.text-danger *
                    .input-group
                        input#nickname.form-control.by-light(type='text', name='nickname', autocomplete='off')
                    small.form-text.text-muted Your nickname will be shown on RANKING BOARD
                br
                .float-left
                    a(href='javascript:history.back()') Back
                .float-right
                    button.btn.btn-secondary(type='submit', tabindex='8') 제출
